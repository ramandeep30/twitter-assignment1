import org.scalatest.FlatSpec


class TwitterFeedsTest extends FlatSpec{

  val twitterFeeds = new TwitterFeeds

  /*it should "return empty list for getTweets for empty input" in{
    assert(twitterFeeds.getTweets("").isEmpty)
  }

  it should "return non empty list for getTweets for empty input" in{
    assert(twitterFeeds.getTweets("#scala").isEmpty)
  }
*/

}
